# ============================================
# Content Creation Crew - Frontend Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# DO NOT commit .env.local to version control
#
# Usage:
#   cp .env.example .env.local
#   # Edit .env.local with your actual values
# ============================================

# ============================================
# REQUIRED - API Configuration
# ============================================

# Backend API URL (REQUIRED in production)
# In development, defaults to http://localhost:8000 if not set
# In production, MUST be set and MUST use HTTPS
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================
# OPTIONAL - OAuth Provider Client IDs
# ============================================
# These are public client IDs (safe to expose in frontend)
# Get these from your OAuth provider dashboards

# Google OAuth Client ID (public)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Facebook OAuth App ID (public)
NEXT_PUBLIC_FACEBOOK_CLIENT_ID=

# GitHub OAuth Client ID (public)
NEXT_PUBLIC_GITHUB_CLIENT_ID=

# ============================================
# NOTES
# ============================================
#
# 1. NEXT_PUBLIC_API_URL:
#    - REQUIRED in production builds
#    - Must use HTTPS in production
#    - Set as build-time environment variable or in .env.local
#
# 2. OAuth Client IDs:
#    - These are PUBLIC and safe to expose in frontend code
#    - OAuth secrets are handled by backend only
#    - Optional - OAuth login will be disabled if not set
#
# 3. Build-time vs Runtime:
#    - NEXT_PUBLIC_* variables are embedded at build time
#    - Changes require rebuild: npm run build
#    - For Docker builds, pass via --build-arg
#
# 4. For Docker:
#    - Set NEXT_PUBLIC_API_URL as build arg in Dockerfile
#    - Or set in docker-compose.yml environment section
#
# ============================================
